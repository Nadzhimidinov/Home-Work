print('Привет, меня зовут Мухмуд')
name = input('А как тебя зовут?: ')
if name.isdigit():
 
 print('Похоже ты меня обманул. Имя не может быть числом. Попробуй заново.')

else:
 name = name.title()
 print(f'{name}, вот и познакомились.')
 age = input('Сколько тебе лет?: ')
 if age.isdigit():
  age = int(age)
  gender = input(f'{name}, какого ты пола? Введи м(Мужского) или ж(Женсокго)').lower()
  if gender == 'м' or gender == 'ж':
    if gender == 'м':
     print(f'{name}, извини но я не общаюсь с людьми мужского пола.')
    else:
     if age < 18:
      print(f'{name}, извини но я не общаюсь с несовершеннолетними девушками.')
     else:
      print(f'Отлично! Тебя зовут {name}, твой возраст {age}, и ты женского пола.\nВведи мне два числа и я покажу тебе сумму, разность и произведение этих чисел!')
      a, b = input('Введи первое число: '), input('Введи второе число: ')
      if a.isdigit and b.isdigit():
       a = float(a)
       b = float(b)
       print(f'''\n
 Введеные числа:
   Первое число: {a}
   Второе число: {b}
   Сумма этих чисел: {a + b}
   Разность этих чисел: {a - b}
   Произведение этих чисел: {a * b}
       ''')
       print(f'{name}, рад был пообщаться! Приходи еще.')
      else:
       print(f'{name}, нужно было ввести числа. Попробуй заново.')
  else:
   print(f'{name}, нужно было ввести м или ж. Попробуй заново.')
 else:
  print(f'{name}, похоже ты меня обманул. Возраст не может быть текстом. Попробуй заново.')
